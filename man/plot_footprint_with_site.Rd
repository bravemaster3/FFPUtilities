% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_footprint_with_site.R
\name{plot_footprint_with_site}
\alias{plot_footprint_with_site}
\title{Plot Footprint Contours with Site Location}
\usage{
plot_footprint_with_site(
  footprint_sf,
  site_coords,
  background_raster = NULL,
  raster_layer_name = "Value",
  raster_colors = c("#00A600", "#F2F2F2"),
  contour_fill = NA,
  contour_color = "black",
  contour_linewidth = 0.5,
  site_color = "red",
  site_size = 3,
  site_shape = 21,
  title = "Footprint Contours",
  xlabel = NULL,
  ylabel = NULL,
  plot_crs_epsg = NULL,
  show_contour_labels = FALSE
)
}
\arguments{
\item{footprint_sf}{sf object or list of sf objects with footprint contours.
Can be output from FFP_to_shapefiles() or a combined sf object.}

\item{site_coords}{Data frame with 'long' and 'lat' columns for tower location
in WGS84 (EPSG:4326), or an sf object with point geometry.}

\item{background_raster}{Optional. A raster object (terra SpatRaster or raster
package) to display as background. Default is NULL.}

\item{raster_layer_name}{Character string. Name for the raster legend.
Default is "Value".}

\item{raster_colors}{Character vector of length 2 specifying low and high
colors for raster gradient. Default is c("#00A600", "#F2F2F2").}

\item{contour_fill}{Color for footprint contours. Default is NA (no fill).}

\item{contour_color}{Color for contour borders. Default is "black".}

\item{contour_linewidth}{Line width for contour borders. Default is 0.5.}

\item{site_color}{Color for site point. Default is "red".}

\item{site_size}{Size of site point. Default is 3.}

\item{site_shape}{Shape of site point (see ?pch). Default is 21 (circle).}

\item{title}{Plot title. Default is "Footprint Contours".}

\item{xlabel}{X-axis label. Default depends on CRS.}

\item{ylabel}{Y-axis label. Default depends on CRS.}

\item{plot_crs_epsg}{Integer. EPSG code for plotting CRS. If NULL, uses CRS
from footprint_sf. Default is NULL.}

\item{show_contour_labels}{Logical. If TRUE and contours have 'contour_pct'
attribute, labels contours with percentage. Default is FALSE.}
}
\value{
A ggplot2 object that can be further customized or saved.
}
\description{
Creates a ggplot2 visualization of footprint contours with the tower/site
location marked. Optionally includes a background raster layer.
}
\examples{
\dontrun{
# Requires footprint shapefiles from FFP_to_shapefiles
# See vignette("footprint-workflow") for complete example

data(site_coords_example)

plot_footprint_with_site(
  footprint_sf = footprint_contours,
  site_coords = site_coords_example,
  title = "Footprint Climatology",
  show_contour_labels = TRUE
)
}
}
