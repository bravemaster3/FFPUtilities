% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FFP_to_shapefiles.R
\name{FFP_to_shapefiles}
\alias{FFP_to_shapefiles}
\title{Convert FFP Climatology Output to Multiple Shapefile Contours}
\usage{
FFP_to_shapefiles(
  FFP,
  site_long,
  site_lat,
  radius_vector,
  project_shapefile = FALSE,
  project_crs_epsg = 3006,
  save_option = "none",
  output_dir = NULL,
  combined_filename = "footprint_combined",
  reverse_order = TRUE
)
}
\arguments{
\item{FFP}{Output list from calc_footprint_FFP_climatology containing
xr and yr arrays for each contour percentage.}

\item{site_long}{Numeric. Site longitude in decimal degrees (WGS84).}

\item{site_lat}{Numeric. Site latitude in decimal degrees (WGS84).}

\item{radius_vector}{Numeric vector of footprint percentages that were used
in the FFP calculation (e.g., c(50, 60, 70, 80)).}

\item{project_shapefile}{Logical. If TRUE, project shapefiles to CRS specified
in project_crs_epsg. Default is FALSE.}

\item{project_crs_epsg}{Integer. EPSG code for projection. Default is 3006
(SWEREF99 TM).}

\item{save_option}{Character string. Options: "none" (default, no saving),
"individual" (save each contour separately), "combined" (save as single file),
or "both" (save individual AND combined).}

\item{output_dir}{Character string. Directory path for saving files.
Only used if save_option is not "none". Default is NULL.}

\item{combined_filename}{Character string. Filename for combined shapefile
(without extension). Only used if save_option is "combined" or "both".
Default is "footprint_combined".}

\item{reverse_order}{Logical. If TRUE, reverse the order of output list so
largest contour is first. Default is TRUE.}
}
\value{
A list of sf objects, one for each contour percentage. Each sf object
  has a 'contour_pct' attribute. If reverse_order = TRUE, list is ordered
  from largest to smallest contour.
}
\description{
Creates spatial polygon shapefiles for each footprint percentage contour
from the output of calc_footprint_FFP_climatology. Optionally saves as
individual files or as a single combined shapefile.
}
\examples{
\dontrun{
# Requires FFP output from calc_footprint_FFP_climatology
# See vignette("footprint-workflow") for complete example

footprints <- FFP_to_shapefiles(
  FFP = FFP,
  site_long = 19.56924,
  site_lat = 64.159996,
  radius_vector = c(50, 60, 70, 80, 90),
  save_option = "combined",
  output_dir = "output/footprints"
)
}
}
